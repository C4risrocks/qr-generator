# Version control
.git
.gitignore

# Local virtual environment and Python caches
.venv
__pycache__/
*.py[co]
.pytest_cache/
.ruff_cache/

# Tests and samples are not needed at runtime
tests/
examples/

# CI configuration
.github/

# Environment files (keep the example so it is not leaked)
.env
.env.*
!.env.example

# Local Docker tooling
Dockerfile
compose.yaml
.dockerignore
